export interface Position {
  x: number;
  y: number;
}

export interface Sound {
  id: string;
  name: string;
  url: string;
  isPlaying: boolean;
}

export interface SoundData {
  id: string;
  name: string;
  url: string;
}